// Placeholder (spec 13 task 9). Task 13 replaces this with the real bank and
// damage upload forms (file picker, 5 MiB client-side cap, PerRowErrors
// breakdown linking to the quarantine queue). Kept as a tiny stub so the
// router and nav have something to point at before task 13 lands.
export function UploadsPage() {
  return <h1 className="text-xl font-semibold text-slate-900">Uploads</h1>
}
