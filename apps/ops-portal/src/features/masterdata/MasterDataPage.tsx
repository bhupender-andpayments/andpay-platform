// Placeholder (spec 13 task 9). Task 12 replaces this with the real vendor
// registry and courier master read views (getVendors, client-side
// type==='COURIER' filter for the courier master). Task 12 may split this
// into tabs within this file, or nested routes under /masterdata/*; either
// way this stub is the swap-in point. Kept as a tiny stub so the router and
// nav have something to point at before task 12 lands.
export function MasterDataPage() {
  return <h1 className="text-xl font-semibold text-slate-900">Master Data</h1>
}
