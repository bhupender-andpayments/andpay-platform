// Placeholder (spec 13 task 9). Task 14 replaces this with the real
// operational actions (batch trigger, status correction, recompose,
// dispatch history) and task 15 wires the step-up-gated destructive actions
// (terminal override, hold release, vendor suspend) alongside them. Kept as
// a tiny stub so the router and nav have something to point at before task
// 14 lands.
export function OperationsPage() {
  return <h1 className="text-xl font-semibold text-slate-900">Operations</h1>
}
