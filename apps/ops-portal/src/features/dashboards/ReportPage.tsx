// Placeholder (spec 13 task 9). Task 10 replaces this with the real reports
// view (getReport, filters, WatermarkBadge, CSV/Excel export). Kept as a
// tiny stub so the router and nav have something to point at before task 10
// lands.
export function ReportPage() {
  return <h1 className="text-xl font-semibold text-slate-900">Reports</h1>
}
