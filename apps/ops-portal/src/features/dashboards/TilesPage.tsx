// Placeholder (spec 13 task 9). Task 10 replaces this with the real report
// tiles view (getTiles / getTileDrilldown, activation tiles rendered empty
// until FR-07). Kept as a tiny stub so the router and nav have something to
// point at before task 10 lands.
export function TilesPage() {
  return <h1 className="text-xl font-semibold text-slate-900">Dashboards</h1>
}
