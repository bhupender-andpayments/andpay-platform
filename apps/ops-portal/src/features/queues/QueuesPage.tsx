// Placeholder (spec 13 task 9). Task 11 replaces this with the real
// quarantine / intake-exception / status-exception queues (tabs, open/all
// toggle, per-row Resolve action). Kept as a tiny stub so the router and
// nav have something to point at before task 11 lands.
export function QueuesPage() {
  return <h1 className="text-xl font-semibold text-slate-900">Queues</h1>
}
